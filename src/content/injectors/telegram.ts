// Telegram Web (K) draft injector. Runs ONLY on web.telegram.org.
// All behavior lives in core.ts; all selectors live in selectors.json.

import { registerInjector } from './core'

registerInjector('telegram')
